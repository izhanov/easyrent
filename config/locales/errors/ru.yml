ru:
  dry_validation:
    errors:
      or: "или"
      array?: "должен быть массивом"

      empty?: "должен быть пустым"

      excludes?: "не может включать %{value}"

      excluded_from?:
        arg:
          default: "не может быть одним из: %{list}"
          range: "не может быть одним из: %{list_left} - %{list_right}"
      exclusion?: "не может быть одним из: %{list}"

      eql?: "должен быть равен %{left}"

      not_eql?: "не может быть равен %{left}"

      filled?: "не может быть пустым"

      format?: "неверный формат"

      number?: "должно быть числом"

      odd?: "должно быть нечетным"

      even?: "должно быть четным"

      gt?: "должно быть больше %{num}"

      gteq?: "должно быть больше или равно %{num}"

      hash?: "должен быть объектом {}"

      included_in?:
        arg:
          default: "должен быть одним из: %{list}"
          range: "должен входить в диапазон: %{list_left} - %{list_right}"
      inclusion?: "должен быть одним из: %{list}"

      includes?: "должен включать %{value}"

      bool?: "должно быть булевым"

      true?: "должно быть истиной"

      false?: "должно быть ложью"

      int?: "должно быть целым числом"

      float?: "должно быть числом с плавающей точкой"

      decimal?: "должно быть десятичным числом"

      date?: "должно быть датой"

      date_time?: "должно быть датой и временем"

      time?: "должно быть временем"

      key?: "отсутствует"

      attr?: "отсутствует"

      lt?: "должно быть меньше %{num}"

      lteq?: "должно быть меньше или равно %{num}"

      max_size?: "длина не может быть больше %{num} символов"

      min_size?: "длина не может быть меньше %{num} символов"

      none?: "не может быть определено"

      str?: "должно быть строкой"

      type?: "должно быть %{type}"

      email?: "почта должна содержать @"

      rules:
        slug:
          invalid_format: "Должен содержать только a-z и -"
        phone:
          invalid_format: "Должен быть в правильном формате"
        contact_phone:
          invalid_format: "Должен быть в правильном формате"
        service_phone:
          invalid_format: "Должен быть в правильном формате"
        price_range:
          price_range_cell:
            from_greater_than_to: "от не может быть больше чем до"
            from_and_to_equal: "от и до не могут быть равны"
            total_days_greater_than_30: "общее количество дней не может быть больше 30"
            from_equal_last_to: "от не может быть равен последнему до"
            from_already_exist: "значение от уже существует в диапазоне"
            from_less_than_last_to: "от не может быть меньше чем последний до"
        mileage_limit:
          only_markup_or_discount: "только наценка или скидка могут быть установлены"

        bank_account_number:
          invalid_format: "Должен содержать KZ и 18 цифр"
        city:
          title:
            uniqueness_violation: "должно быть уникальным"
        brand:
          title:
            uniqueness_violation: "должно быть уникальным"
        mark:
          title:
            uniqueness_violation: "должно быть уникальным"
        offer:
          prices:
            empty_prices: "должен содержать диапазон цен"
        client:
          identification_or_phone_number:
            uniqueness_violation: "должно быть уникальным"
        starts_at:
          in_the_past: "не должно быть в прошлом"
        ends_at:
          in_the_past: "не должно быть в прошлом"
          less_than_starts_at: "не должно быть меньше чем начало"
        password_confirmation:
          not_equal_to_password: "пароль подтверждения должен совпадать с паролем"
        car_park:
          uniqueness_violation: "БИН/ИИН и префикс заявки должны быть уникальными"
        booking:
          status:
            invalid: "неверный статус"
        pledge_amount:
          less_than_zero: "не может быть меньше нуля"
      size?:
        arg:
          default: "размер должен быть %{size}"
          range: "размер должен быть в диапазоне %{size_left} - %{size_right}"

        value:
          string:
            arg:
              default: "длина должна быть %{size}"
              range: "длина должна быть в диапазоне %{size_left} - %{size_right}"

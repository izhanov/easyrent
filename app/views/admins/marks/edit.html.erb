<div class="d-flex justify-content-between align-items-center">
  <h1><%= t(".title", mark: @mark.title) %></h1>
  <%= link_to admins_mark_path(@mark), class: "btn btn-sm btn-primary" do %>
    <i class="bi bi-box-arrow-left"></i>
  <% end %>
</div>

<div class="container">
  <%= render "form", mark: @mark %>
</div>

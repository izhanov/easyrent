<div class="d-flex justify-content-between align-items-center">
  <h1><%= t(".title") %></h1>
  <div>
    <%= link_to t(".back"), admins_catalogues_path, class: "btn btn-primary" %>
    <%= link_to t(".new_mark"), new_admins_mark_path, class: "btn btn-primary" %>
  </div>
</div>

<div class="container">
  <% if @marks.any?%>
    <%= render @marks %>
  <% else %>
    <p><%= t(".no_marks") %></p>
  <% end %>
</div>

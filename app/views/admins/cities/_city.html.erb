<%= link_to admins_city_path(city), class: "link text-decoration-none" do %>
  <div class="col-sm-6">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title"><%= city.title %></h5>
        <p class="card-text"><%= city.slug %></p>
      </div>
    </div>
  </div>
<% end %>

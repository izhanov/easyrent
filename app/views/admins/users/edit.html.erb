<div class="d-flex justify-content-between align-items-center">
  <h1><%= t(".title", user: @user.full_name) %></h1>
  <%= link_to t(".back"), admins_user_path(@user), class: "btn btn-primary" %>
</div>

<div class="container">
  <%= render "form", user: @user %>
</div>

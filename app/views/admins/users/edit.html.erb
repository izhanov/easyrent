<div class="d-flex justify-content-between align-items-center">
  <h1><%= t(".title", user: @user.full_name) %></h1>
  <%= link_to admins_user_path(@user), class: "btn btn-sm btn-primary" do %>
    <i class="bi bi-box-arrow-left"></i>
  <% end %>
</div>

<div class="container">
  <%= render "form", user: @user %>
</div>

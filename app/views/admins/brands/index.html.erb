<div class="d-flex justify-content-between align-items-center">
  <h1><%= t(".title") %></h1>
  <div>
    <%= link_to t(".back"), admins_catalogues_path, class: "btn btn-primary" %>
    <%= link_to t(".new_brand"), new_admins_brand_path, class: "btn btn-primary" %>
  </div>
</div>

<div class="container">
  <% if @brands.any?%>
    <%= render @brands %>
  <% else %>
    <p><%= t(".no_brands") %></p>
  <% end %>
</div>

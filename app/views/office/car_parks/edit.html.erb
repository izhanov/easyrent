<div class="d-flex justify-content-between align-items-center">
  <h1 class="text-align-center">
    <%= t(".title", car_park: @car_park.title) %>
  </h1>

  <%= link_to office_user_car_park_path(current_office_user, @car_park), class: "btn btn-outline-primary btn-sm"  do %>
    <i class="bi bi-box-arrow-left"></i>
  <% end %>
</div>


<div class="d-flex justify-content-around">
  <div class="col-3">
    <%= link_to office_user_car_park_price_range_path(current_office_user, @car_park, @car_park.price_range), class: "btn btn-sm btn-warning"  do %>
      <i class="bi bi-bar-chart-steps"></i>
      <%= t(".price_ranges") %>
    <% end %>
  </div>

  <div class="col-3">
    <%= link_to office_user_car_park_price_range_path(current_office_user, @car_park, @car_park.price_range), class: "btn btn-sm btn-warning"  do %>
      <i class="bi bi-bar-chart-steps"></i>
      <%= t(".seasons") %>
    <% end %>
  </div>

  <div class="col-3">
    <%= link_to office_user_car_park_additional_services_path(current_office_user, @car_park), class: "btn btn-sm btn-warning"  do %>
      <i class="bi bi-bar-chart-steps"></i>
      <%= t(".services") %>
    <% end %>
  </div>
</div>

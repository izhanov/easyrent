<div class="d-flex justify-content-between align-items-center">
  <h1><%= t(".title") %></h1>
  <%= link_to office_car_park_path(@car_park), class: "base-button base-button--info base-button--sm" do %>
    <%= t(".back") %>
  <% end %>
</div>


<div class="container">
  <%= render "form", car: @car, owner: @car_park, back_url: office_car_park_path(@car_park) %>
</div>

<div class="d-flex justify-content-between align-items-center">
  <h1>
    <%= @car.mark_title %>
    <span class="text-secondary small fs-5 font-monospace"><%= @car.plate_number %></span>
  </h1>
  <div>
    <%= link_to office_car_park_path(@car_park), class: "btn btn-sm btn-outline-primary" do %>
      <i class="bi bi-box-arrow-left"></i>
    <% end %>
    <%= link_to edit_office_car_park_car_path(@car_park, @car), class: "btn btn-sm btn-warning" do %>
      <i class="bi bi-pencil-square"></i>
    <% end %>
    <%= link_to office_car_park_car_path(@car_park, @car), class: "btn btn-sm btn-danger", data: {turbo_confirm: t(".sure?"), turbo_method: :delete} do %>
      <i class="bi bi-trash"></i>
    <% end %>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-6">
      <% @car.photos.each do |photo| %>
        <%= link_to photo.image.url, target: "_blank" do %>
          <%= image_tag photo.image(:small).url, class: "img-thumbnail" %>
        <% end %>
      <% end %>
    </div>

    <div class="col-6">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Тарифы</h5>
          <p class="card-text">
            Добавьте тарифы для автомобиля.
          </p>

          <%= link_to office_car_park_car_offers_path(@car_park, @car), class: "btn btn-sm btn-warning"  do %>
            Перейти к тарифам
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="d-flex justify-content-between align-items-center">
  <h1><%= t(".title") %></h1>
  <%= link_to office_car_park_additional_services_path(@car_park), class: "btn btn-sm btn-outline-primary" do %>
    <i class="bi bi-box-arrow-left"></i>
  <% end %>
</div>

<%= turbo_frame_tag @additional_service do %>
  <%= render "form", additional_service: @additional_service %>
<% end %>

['turbo:load', 'turbo:visit'].forEach((eventName) => {
  document.addEventListener(eventName, (e) => {
    const carParkSelect = document.querySelector('select[id="car_park_id"]');
    if (carParkSelect) {

    }
  });
});
